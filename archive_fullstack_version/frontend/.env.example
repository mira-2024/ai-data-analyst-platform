# Copy to .env.local and fill in values.
# VITE_* variables are embedded at build time (not secret in browser bundles).

# API key — must match API_KEY in backend/.env
# Leave empty to disable auth check in dev (when backend API_KEY is also empty)
VITE_API_KEY=
